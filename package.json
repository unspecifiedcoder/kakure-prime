{
  "name": "kakure",
  "private": true,
  "version": "0.1.0",
  "description": "Kakure: shielded FROST-multisig treasury on Solana",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "tsup": "^8.3.0",
    "@types/node": "^22.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "bigint-buffer",
      "blake-hash",
      "bufferutil",
      "esbuild",
      "leveldown",
      "msgpackr-extract",
      "utf-8-validate"
    ]
  }
}
