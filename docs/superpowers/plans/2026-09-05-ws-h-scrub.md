# Workstream H — scrub prior company/project references

Baseline: main `b9ff5dd` (green e2e, 9/9). Working tree: `~/kakure-wt/h-scrub`, branch `ws/h-scrub`.

Rule (final, per owner clarification): the old project/company codename must appear NOWHERE in the
working tree (case-insensitive; content, comments, docs, metadata, fixtures, filenames). The word
"darkpool" is acceptable only as a generic noun for "the reference EVM implementation" and is
removed wherever convenient. Replacement vocabulary: company/product = **Kakure**; source EVM
project = "the reference EVM implementation" (no name/URL/commit/org); previous author string =
`"Kakure Labs"`.

Status: DONE. All categories below are complete; the zero-match grep passes.

## (a) docs / comments / metadata

- [x] `README.md`, `circuits/README.md`, `circuits/PROVENANCE.md`, `circuits/vendor/PROVENANCE.md`
- [x] `circuits/*/Nargo.toml` authors -> `["Kakure Labs"]` (shared, standard/{deposit,transfer,withdraw},
      multisig/{transfer,split,join,withdraw}_multisig)
- [x] `packages/coordinator/package.json`, `packages/indexer/package.json` author field
- [x] `docs/superpowers/plans/2026-09-05-kakure-master-plan.md`,
      `docs/superpowers/specs/2026-09-05-kakure-design.md`
- [x] Source-comment mentions of the old package scope across `packages/sdk/src/**`
      (`tx/index.ts`, `__tests__/derivedEph.test.ts`, `state/PersistentEphemeralCounterStore.ts`)

## (b) cryptographic domain-separator strings

Every `<old>.<x>` string literal became `kakure.<x>` (same suffix/version tag), in both Noir
(`circuits/shared/src/common/domains.nr`) and TypeScript (`packages/sdk/src/crypto/constants.ts`,
`packages/sdk/src/tss/domains.ts`). Hardcoded hex constants were recomputed with
`circuits/scripts/gen_domains.mjs` (`keccak256(label) mod BN254_Fr`):

- [x] ENC_DOMAIN, PSI_DOMAIN, SCHNORR_DOMAIN, ACTION_WITHDRAW/TRANSFER/SPLIT/JOIN (Noir + TS, byte-identical)
- [x] TS-only FROST protocol domains: FROST_RHO_DOMAIN, FROST_NONCE_DOMAIN, FROST_MSG_DOMAIN,
      FROST_COM_DOMAIN, FROST_POP_DOMAIN, CP_DOMAIN

Runtime-derived labels (string rename alone was sufficient, no hex to recompute):

- [x] `packages/sdk/src/note/keys.ts` (VIEW/IN_KEY/SELF_EPH/SELF_SPEND/PUB_IN labels)
- [x] `packages/sdk/src/frost/multisigNote.ts`, `frost/groupViewKey.ts`
- [x] `packages/sdk/src/keys/SolanaAccount.ts` (root / PSS-state labels)
- [x] `packages/sdk/src/merkle/genesis.ts` (GENESIS_DOMAIN)
- [x] `packages/sdk/src/tss/gjkr.ts` (Pedersen H seed)
- [x] Mnemonic/spend/view KDF purpose-labels exercised by `domains.test.ts` / `Kdf.test.ts`

## (c) non-crypto identifiers

- [x] `packages/sdk/src/address.ts`: address type/function names and the on-wire address prefixes
      renamed to a Kakure-branded family (`Kakure*`, `kak_`/`kakpub_`), plus every importing test
- [x] Package-scope references in comments (`packages/sdk/src/tx/index.ts`,
      `__tests__/derivedEph.test.ts`, `state/PersistentEphemeralCounterStore.ts`)

## (d) KAT / fixture regeneration

Every fixture whose expected value derived from a renamed domain string was regenerated by running
the actual TS/Noir implementation (never hand-edited):

- [x] TS unit KATs: `domains.test.ts`, `string_to_fr.test.ts`, `Kdf.test.ts`, `dem.test.ts`,
      `note.test.ts`, `nullifier.test.ts`, `keys.test.ts`, `gen-m-kat.test.ts`, `gen-frost-kat.test.ts`,
      `lifecycle.test.ts` (deposit/memo ciphertexts, re-derived + re-encrypted), `multisig-note.test.ts`,
      `publicAddress.test.ts`
- [x] Noir FROST fixtures for all four multisig circuits (`main.nr` `KAT_*` globals/`kat_*` fns),
      regenerated via new `circuits/scripts/gen_frost_kats.ts` (generalizes the
      `gen-*-multisig-kat.test.ts` pattern)
- [x] Noir standard-circuit fixtures: `shared/src/common/test_fixtures.nr`, `shared/src/harness.nr`,
      `shared/src/common/dem.nr`, `shared/src/common/note.nr`, `shared/src/common/note_nullifier.nr`,
      `shared/src/multisig/frost.nr`, and `standard/{deposit,transfer,withdraw}/src/{main,mutation_tests}.nr`
- [x] Circuits rebuilt end to end (`just build-circuits`: nargo compile -> sunspot compile/setup/deploy,
      all 7 circuits), `circuits/manifest.json` regenerated, `just verify-kat` real Groth16
      prove+verify green, `circuits/kat/proof_compression_kat.json` regenerated from the new
      `transfer_multisig.proof` via the `tests-litesvm` generator test
- [x] `tests-litesvm` fixtures/tests all green against the rebuilt `.so`s (no embedded stale bytes;
      they read `circuits/target`/`target/deploy` live)
- [x] Confirmed genuinely domain-independent KATs (`LeanImtSharedKat`, the pool's LeanIMT KAT,
      `proof_compression_kat_g2_regression.json`) pass UNCHANGED, as expected

## Verification gate

- [x] `pnpm -r test` per package (`sdk`, `prover`, `cli`, `coordinator`, `indexer`, `apps/dashboard`),
      `--no-file-parallelism` — all green
- [x] `nargo test --workspace --silence-warnings` — all green
- [x] `cargo test --lib` (`programs/kakure_pool`) — green
- [x] `cargo test --release` (`tests-litesvm`) — all 19 tests green, including the real-verifier CPI test
- [x] `just e2e-scenario` (foreground) — 9/9 green
- [x] Final zero-match grep for the old codename (case-insensitive, all extensions, filenames included)
- [x] New circuit artifacts (`.so`/`.pk`/`.ccs`/`.json`/`.vk`) copied into `~/kakure/circuits/target/`
      so main's e2e can find them; `manifest.json` and `proof_compression_kat.json` copied too
