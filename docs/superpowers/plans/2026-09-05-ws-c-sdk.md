# Workstream C — SDK port (`packages/sdk`)

Source of truth: `the reference EVM implementation's wallets package` (read-only, commit `3e7333c`). Frozen
interfaces I-1, I-2, I-3, I-5, I-6, I-8 in the master plan are binding.

Each task: failing test -> run -> implement -> run green -> commit.

## 1. Package scaffold
- [x] Create `packages/sdk` with package.json (`@kakure/sdk`), tsconfig.json,
      tsconfig.typecheck.json, vitest.config.ts, tsup.config.ts modeled on
      the reference EVM implementation's wallets package.
- [x] `pnpm install` at worktree root resolves workspace.
- [x] Commit scaffold.

## 2. Copy chain-agnostic modules + tests unchanged
- [x] Copy `frost/`, `tss/`, `threshold/`, `note/`, `merkle/LeanIMT.ts`,
      `discovery/`, `tx/plan.ts`, `tx/assemble.ts`, `tx/witnessSources.ts`,
      `tx/index.ts`, `crypto/` (minus fields.ts), `utxo/`, `state/`,
      `interfaces.ts`, `repositories.ts`, `address.ts`, plus their tests
      from the reference EVM implementation's wallets package/src.
- [x] Drop EVM-coupled: `crypto/fields.ts`, `merkle/genesis.ts`,
      `keys/DarkAccount.ts`, `public/*`, `sync/PublicMemoScanner.ts`,
      `sync/ScanEngine.ts`, `tx/ports.ts` (replaced/dropped per spec).
- [x] Run `pnpm --filter @kakure/sdk test` — copied tests green (imports
      adjusted; one real bug found and fixed: `Poseidon.test.ts` still
      referenced the dropped `addressToFr`). Preserves the reference EVM implementation KATs for
      Poseidon2 note/nullifier/FROST.
- [x] Commit.

## 3. LeanIMT hash -> Poseidon v1 (circom/BN254 params)
- [x] `circuits/kat/lean_imt_poseidon_v1.json` confirmed absent (checked both
      at the worktree root and in the `a-circuits` worktree). Since no
      external oracle exists yet, `LeanImtParity.test.ts` was rewritten to
      check the frontier-insert algorithm against an independent
      from-scratch rebuild (same rule, no shared state) instead of hardcoded
      Poseidon2 roots, which no longer apply. `PoseidonV1.test.ts` records
      the reconciliation flag for workstream A explicitly.
- [x] Implemented: `crypto/PoseidonV1.ts` (circomlibjs, ambient .d.ts added),
      `merkle/LeanIMT.ts`'s node hash swapped, zero-sibling passthrough and
      canonical-index assert unchanged. `tx/witnessSources.ts`'s `foldPath`
      (which folds the same tree) swapped too.
- [x] Tests green.
- [x] Committed.

## 4. `src/solana/` module
- [x] Tests for `assetId(mint)`, `recipientField(dest)`, PDA seeds (I-2),
      `genesisLeaf(genesisHash)`, `TxBuilder` in `__tests__/solana.test.ts`.
- [x] Implemented per spec §3.2/§3.3 and I-2 seeds.
- [x] `IndexerClient` implementing `ChainView` over I-8 endpoints (fetch-based).
- [x] `TxBuilder`: builds each I-3 instruction from a `ProofBundle` (I-6),
      prepends `ComputeBudgetProgram.setComputeUnitLimit(1_400_000)`.
- [x] IDL stub at `src/solana/idl/kakure_pool.json` (confirmed absent in
      `b-program` worktree as of this port) — minimal stub matching I-2/I-3,
      marked `"__stub": true`, with a `__note` explaining account lists are
      C's best-effort reconstruction, not authoritative.
- [x] Tests green, committed.

## 5. `keys/SolanaAccount.ts`
- [x] Test: seed = ed25519 signature (via `@noble/curves/ed25519`) over
      `"kakure.account.v1"` by a Solana keypair; derived key hierarchy
      (`note/keys.ts`) untouched, so it matches `DarkAccount`'s shape exactly.
- [x] Implemented, replacing `DarkAccount.ts`. Kept a `fromSeed` deterministic
      test-only constructor (mirrors the reference EVM implementation's `fromMnemonic` escape hatch)
      so the ~10 ported tests that built fixed test accounts did not need a
      real Keypair + signature in every case.
- [x] Tests green, committed.

## 6. `sync/ScanEngine.ts` port
- [x] Test: `ScanEngine.test.ts` — single-key discovery of a self-minted note
      over a fake I-8 `NotesTransport`, foreign-tag rejection, empty-feed
      smoke test. Multisig (group view key) scanning is `MultisigScanEngine`,
      a thin transport adapter over the already-extensively-tested
      `frost/multisigScan.ts` `MultisigScanner` (28 cases in
      `multisig-note.test.ts`) — not re-tested here to avoid duplicating that
      coverage; flagged in the final report as a coverage gap on the adapter
      itself.
- [x] Implemented using `frost/multisigScan.ts` (ported, `MultisigScanner`)
      for group scanning.
- [x] Tests green, committed.

## 7. Public API
- [x] `src/index.ts` exports a clean surface; no `any` in exported types
      (verified: `grep -rln ": any\b"` over non-test `src/` is empty).
- [x] Every file this workstream authored or touched typechecks clean under
      `tsconfig.json` (strict, `noUncheckedIndexedAccess`,
      `exactOptionalPropertyTypes`). ~74 pre-existing strictness gaps remain
      in files copied verbatim from the reference EVM implementation (whose own tsconfig lacks those
      two flags) — left unfixed and flagged rather than patched with guessed
      non-null assertions in crypto code; vitest (esbuild) ignores types so
      runtime correctness is unaffected.
- [x] Committed.
